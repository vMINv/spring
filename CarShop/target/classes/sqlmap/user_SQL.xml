<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE  mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="users">

	<insert id="insert"
		parameterType="com.carshop.users.User" useGeneratedKeys="true"
		keyProperty="username">
		<![CDATA[
			INSERT INTO users
			(username, password, authority, enabled, uname, uemail)
			VALUES
			(#{username}, #{password}, "USER", 1, #{uname}, #{uemail})
		]]>
	</insert>
	
	<select id="select_list"
		resultType="com.carshop.users.User"><!-- 전부 -->
		<![CDATA[
			SELECT * FROM users ORDER BY uno DESC
		]]>
	</select>
	
	<update id="updateAuth" 
		parameterType="hashMap"><!-- 여러개 (한개X, 전부X) -->
		<![CDATA[
			UPDATE users 
			SET authority = #{authority}
			WHERE username = #{username}			
		]]>
	</update>
	
	<delete id="delete"
		parameterType="String"><!-- 한개 -->
		<![CDATA[
			DELETE FROM users
			WHERE username = #{username}
		]]>
	</delete>
	
	</mapper>